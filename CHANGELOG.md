# Component Warehouse 更新日志

## 2026-06-13

### 元器件库完整改善

- 默认分类新增“时钟源”，晶振、振荡器、谐振器由 AI 整理归入该类，修复晶振停留在“其他”的问题。
- 分类、命名、`part_family`、`normalized_spec` 等语义整理以接入 AI 为准；AI 不可用时保留原始字段并进入待整理/重试队列。
- 导入来源统一标记：立创商城 Excel、图片识别导入、手动新增等来源会在卡片、详情和表格中显示。
- 元器件卡片标题重新规范：电阻/电容/电感突出阻值、容值、感值，型号和封装放入副标题或标签；连接件统一展示排针、排母、端子等规格。
- 首页改为简洁仪表盘，移除提醒/通知/AI 后台控制区，修复最近项目 BOM 数量和缺料状态显示不准的问题。
- 新增“覆盖图”页面，用对数轴展示电阻、电容、电感规格覆盖分布和库存点位。
- 元器件库改为分页分组加载，默认 24 个/页，并预计算展示字段，减少滚动时重复 JSON 解析和 DOM 压力。
- 搜索无结果时显示明确空态、清空筛选、立创搜索和 AI 相近型号建议，不再出现空白页。
- 后台 AI 自动维护 pending/stale/failed 任务，并温和刷新较旧的 AI 建议。
- 元器件卡片、表格、移动卡片和详情页新增“一键复制型号”，优先复制 `model`，方便粘贴到 EDA。
- BOM 导入预览升级为 AI 库存分拣工作台，自动分为“库内已匹配 / 需确认 / 待采购”，可导入已选择项并复制采购关键词。
- MiMo 请求超时改为 `MIMO_TIMEOUT_SECONDS`，默认 90 秒；BOM AI 匹配使用瘦身候选字段，失败时保留库存预匹配结果并显示可读错误。

### BOM 匹配可视化

- 项目保存最近一次 BOM 导入匹配快照，记录总行数、库内已匹配、需确认、待采购和待采购清单。
- 项目 BOM 页新增匹配进度仪表盘，用大百分比、三段进度条和指标卡展示 BOM 与库存的匹配情况。
- BOM 导入弹窗顶部新增比例进度条，导入前即可看到已匹配、需确认、待采购占比。
- 首页新增项目 BOM 匹配概览，最近项目卡片显示匹配率、待采购数量和轻量分段条。
- 匹配可视化使用 CSS 分段条实现，不额外创建大量图表实例，保持页面滚动流畅。
- 首页升级为项目优先的大仪表盘，新增 BOM 匹配堆叠图和库存占用环图，旧项目会先用正式 BOM 满足率兜底展示。
- 元器件库顶部入口简化，AI 分类整理和重置收进维护菜单，日常界面只保留导入、新增和查询入口。
- 元器件库筛选区和卡片调整为更轻的 Apple 风格：柔和半透明面板、统一圆角、轻微 hover 动效，避免重阴影影响滚动。
- BOM 导入批次改为数据库表保存，解析行、匹配候选、确认状态和 AI 建议都可刷新后继续处理，不再依赖整批 JSON 字段。
- 立创编号完全一致的 BOM 行会标记“编号一致”并自动导入正式项目 BOM；重复上传同一行不会重复占用。
- 电阻、电容、电感匹配新增标称值和封装安全校验，值或封装不一致不会自动匹配，并会给出待确认/待采购与组合替代建议。
- BOM 匹配弹窗改为居中工作台布局，头部和底部固定、内容内部滚动，避免按钮或关闭入口被视口遮挡。
- BOM 匹配进一步改为立创编号优先：BOM 中有立创编号时，只有编号一致才算库内完全匹配；编号未入库会进入待采购，并把库内同值/同封装或相似物料作为替换提醒，避免误判和重复采购。
- BOM 导入批次行新增“忽略此项”，用于 `NS`、测试点、不装件等导出噪声；忽略后该行不再计入匹配进度和待采购统计，并持久化到数据库。
- 前端改为路由懒加载，移除全量图标注册，ECharts 改成按图表类型加载；入口主 JS 从约 2.56MB 降到约 1.09MB，项目页、元器件页、覆盖图拆成独立小 chunk，提升页面打开和切换流畅度。
- 修复 BOM 匹配中阻容感数值解析误伤封装和型号的问题，例如 `0805 10kΩ` 不再被拼成 `80.51MΩ`，电容 `C0805 1uF` 不再被误算为 `80.547nF`。
- BOM 确认页的“BOM 指定”列改为优先显示值/参数，型号、封装和立创编号放在副信息中；待确认下拉支持“更多库存”和远程搜索，可从完整元器件库手动选择。
- 项目 BOM 卡片新增匹配来源标记，区分“编号一致”和“手动匹配”。
- BOM 缺料行支持加入“待采购库”：自动创建库存数量为 0 的待采购占位物料，写入立创编号、型号、封装和 BOM 位号，并同步匹配到项目 BOM，方便后续购买入库。
- 项目页弹窗增加关闭销毁和稳定 row-key，减少 BOM 分拣表格、库存选择表格长期挂载造成的卡顿。
- 修复 BOM 待采购行手动选择库内物料后仍停留在“待采购”的问题；现在选择后会立即标记“手动匹配”，并计入库内已匹配和待确认导入。
- 立创订单 Excel 导入命中“待采购库”占位物料时，会自动合并到该物料、补充库存并把状态从“待采购”转为“在库”，项目 BOM 缺料会随库存自动抵消。

## 2026-06-12

### 选型工作台迭代

- 新增“关于”页面，左侧导航可进入，集中展示版本更新、引用信息、系统统计、登录日志和变更日志。
- 登录接口现在会记录成功/失败日志，不保存密码，仅记录认证动作结果。
- 新增公网基础安全加固：登录失败限流、安全响应头、Trusted Host 白名单、默认隐藏 API 文档、Nginx 禁止 dotfile 访问。
- 新增元器件规范化流程：规则提供分类边界，MiMo 负责总结规范短名称、分类、标签和规格，原始购物标题保存为 `source_title`。
- 新增“连接件”分类，统一收纳排针、排母、插座、端子、线束、螺丝、螺母和铜柱，并继续用 `part_family` 区分细类。
- 元器件卡片新增电阻、电容、电感常用单位换算，帮助快速理解 Ω/kΩ/MΩ、pF/nF/µF、nH/µH/mH。
- 元器件卡片移除“AI 已完成”徽标，只在待整理、整理中、需更新或失败时显示 AI 状态，避免和物料状态混淆。
- 首页“低库存提醒”改为“库存行动建议”，优先提示项目缺料、常用耗材不足、资料缺失和 AI 失败项，开发板等单件库存不再默认霸榜。
- BOM 导入匹配保留低置信候选，不再让未匹配行空着；缺失项会显示可能作用、缺料说明和立创搜索入口。
- BOM 页面物料作用说明不再截断为省略号；点击物料名可跳转到元器件库并自动搜索对应物料。
- 全局侧边栏和 BOM 项目侧栏支持常驻与收起，减少页面滚动时导航丢失的问题。
- 新增立创商城公开搜索跳转，元器件、缺失物料和 AI 推荐型号可直接打开 `m.szlcsc.com` 搜索。
- 新增 AI 图片识别导入预览接口和前端入口，支持购物截图识别候选物料；结果默认需要人工确认，不自动写库。
- 图片识别候选项支持匹配已有库存、合并数量、新增或跳过；低置信度和非购物截图默认跳过。
- 扩展小件字段：`part_family`、`count_mode`、`normalized_spec`，用于排针、螺丝、螺母、铜柱和线材的粗分类统计。
- 首页改为行动导向布局，突出可用库存、项目占用、低库存、待 AI、缺数据手册和机械件规格。
- BOM 单项状态重新梳理：单项只保留预占/待取料、已取料、已释放；项目层面新增“完成项目”操作。
- 旧 BOM 单项 `done` 状态保留兼容，前端显示为“旧状态：已完成”，可手动转换为已取料并扣库。
- BOM 卡片新增元器件作用说明，优先显示 BOM 备注，其次显示 AI 用途一句话。
- 项目规划和 BOM 分析改为 Markdown 可读展示，不再直接展示原始 JSON。
- 新增项目 AI 咨询接口，围绕当前 BOM 提问时优先匹配已有库存，缺失项提供立创搜索入口。
- 元器件库卡片减少大段 AI 文本，改为关键参数 chips、一句话用途和立创搜索按钮。
- 统一按钮高度、标签对齐和移动端工具栏布局，减少手机端溢出。

### AI 知识卡片升级

- 升级元器件 AI 分析提示词，要求输出面向硬件设计决策的内容，不再简单复述名称、阻值、容值、封装等已知信息。
- 新增“设计洞察”类内容，重点覆盖降额建议、误用边界、参数核对、布局注意和替代料检查项。
- 针对常见器件类型加入更细的分析方向：
  - 电阻：功耗裕量、工作电压/电流估算、分压阻抗对 ADC/偏置的影响、TCR 和脉冲功率核对。
  - 电容：MLCC DC Bias、介质/温漂、耐压降额、ESR/纹波、去耦和储能边界。
  - MOS、电源、接口、芯片：关键数据手册参数、典型外围、热设计、封装电流限制和替代核对项。
- AI 结果新增结构化字段：`design_insights`、`datasheet_notes`、`do_not_use_for`、`recommended_pairings`、`source_notes`、`substitution_notes`。
- 引入 `AI_ANALYSIS_VERSION` 到缓存 key，旧版浅层 AI 缓存会自动标记为过期并后台重跑一次；缓存 key 不变时不会重复调用 MiMo。
- 前端元器件详情抽屉重构 AI 区域，按“设计洞察 / 不适合场景 / 手册核对项 / 推荐搭配 / 风险提示 / PCB 注意 / 替代料检查 / 依据说明”分组显示。
- 前端会自动解析 AI JSON、数组和纯文本，避免直接展示带引号和方括号的原始 JSON。
- 详情抽屉新增重点参数芯片，会优先显示 AI 关键规格；旧数据则从名称、参数、封装、标签中自动提取阻值、容值、频率、精度、耐压、电流、功率、输入/输出范围等信息。
- AI 知识块加入彩色分组和左侧色条，风险、PCB 注意、手册核对、替代料检查等信息更容易扫读。
- 明确资料来源策略：会员订单页依赖登录态，不能作为后台稳定抓取来源；AI 优先使用库存字段、立创编号、手动数据手册链接、公开立创商品页、厂商官网和官方数据手册。

### 元器件卡片列表

- 电阻、电容、晶振等被动器件卡片优先突出阻值、容值或频率，型号和封装降为副标题显示。
- 修复分类卡片视图顺序受 `updated_at` 影响的问题。AI 后台整理更新摘要后，卡片不会再因为更新时间变化而重新洗牌。
- 分类卡片接口现在使用稳定排序：分类按默认分类顺序，类目内按元器件 ID 固定。
- 分类卡片接口补齐库存、封装、标签和 AI 标签过滤参数，卡片视图与表格视图筛选行为更一致。

### AI 后台任务

- 后台 worker 默认自动运行，服务启动后会扫描缺少 AI 信息或缓存过期的元器件并加入队列。
- 同一元器件在缓存 key 未变化时不会重复调用 AI；字段变化或 AI 分析版本升级时才会重新整理。
- AI 生成结果自动写入 AI 字段和知识卡片，但不会覆盖库存数量、型号、位置等用户核心字段。

### UI 与可读性

- 详情抽屉 AI 内容改为知识笔记式卡片，提升长文本可读性。
- 隐藏空 AI 分组，避免旧数据或待整理数据出现空白说明块。
- 保留浅色高级卡片风格，适配桌面和手机布局。

## 2026-06-11

### 第一版核心系统

- 完成 Component Warehouse 基础系统：FastAPI 后端、SQLite 数据库、Vue 3 + Vite + Element Plus 前端。
- 实现元器件新增、编辑、删除、查询、分类筛选、状态筛选、标签筛选、关键词搜索。
- 初始化默认分类：电阻、电容、电感、二极管、三极管、MOS管、芯片、电源、接口、开关、开发板、保护器件、传感器、其他。
- 实现项目 BOM 管理：创建项目、添加 BOM 元器件、库存满足判断、缺料清单和 BOM 导出。
- BOM 项目使用库存占用机制，未取料项目会占用库存，避免多个项目重复使用同一批库存。
- BOM 不再直接删除作为主要流程，支持标记为已完成或已取料。
- 元器件支持手动快捷扣减数量。

### Excel 与 BOM 导入

- 支持 `.xls` 和 `.xlsx` 文件导入。
- 支持立创商城物料明细对账单表头解析。
- 导入前提供预览和处理动作选择。
- 按“订单编号 + 商品编号/立创编号”记录导入历史，重复订单物料默认跳过，避免重复累加库存。
- 支持项目 BOM 表导入后自动匹配库存元器件，未匹配项做近似匹配建议。

### AI 能力

- 接入小米 MiMo OpenAI 兼容接口，默认模型 `mimo-v2.5`。
- API Key 仅通过 `.env` 或部署环境变量 `MIMO_API_KEY` 注入，不写入代码仓库。
- 实现 AI 分类、用途说明、项目规划、需求找料、器件规格说明、BOM 分析等接口。
- 需求找料会先检索本地库存，再让 MiMo 输出库存命中、可替代库存、缺失采购建议和风险提示。
- 器件说明支持可选联网搜索：关闭、自动、强制。
- AI 结果可保存为知识卡片，并融入元器件和 BOM 页面，不再依赖单独 AI 页面作为主要入口。

### 首页、日志与部署

- 首页显示库存总览、低库存、待验证器件、最近项目、分类图表、状态图表和 AI 任务摘要。
- 新增操作日志 API，记录导入、元器件使用、库存扣减、项目/BOM 操作等关键事件。
- 预留外部 APP 调用 API。
- 支持 Docker Compose 部署。
- 支持 `/cw/` 子路径部署，适配 `https://wxylab.ltd/cw/`。
- 新增简单本地访问保护和登录页。
- 初始化 Git 仓库，忽略 `.env`、数据库、依赖目录和构建产物。
